#include <bits/stdc++.h>

using namespace std;

int main(){
    string s;
    set<string> se;
    while(cin >> s){
        se.insert<s>;
    }
    cout << se.size() << "\n"; 
    auto it == se.end();
    it--;
    cout << *se.begin() << " " << *it <endl;
    return 0;
}